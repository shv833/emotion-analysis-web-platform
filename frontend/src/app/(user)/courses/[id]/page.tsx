import React from 'react'

type Props = {
  params: { id: string }
  searchParams: { [key: string]: string | string[] | undefined }
}

const CourseDetail = async ({ params, searchParams }: Props) => {
  return <div className=""></div>
}

export default CourseDetail
